{% if collection.metafields.custom.collection_faq_item != blank %}
<div class="collection-faq-accordion container">
  {% if section.settings.section_title != blank %}
    <h2 class="collection-faq-section-title">{{ section.settings.section_title }}</h2>
  {% endif %}
  
  {% for block in collection.metafields.custom.collection_faq_item.value %}
    <div class="collection-accordion-item {% if forloop.first %}active{% endif %}">
      <div class="collection-accordion-header">
        {{ block.question }}
        <span class="collection-accordion-icon">+</span>
      </div>
      <div class="collection-accordion-panel">{{ block.answer }}</div>
    </div>
  {% endfor %}
</div>
{% endif %}

<style>
.collection-faq-accordion {
  max-width: 800px;
  margin: 0 auto;
  font-family: sans-serif;
}
.collection-faq-section-title {
  text-align: center;
  font-size: 28px;
  font-weight: 600;
  margin-bottom: 30px;
  color: #166e5f;
}
.collection-accordion-item {
    margin-bottom: 15px;
    box-shadow: rgba(0, 0, 0, 0.1) 0px 4px 8px;
    border-top-left-radius: 10px;
    border-top-right-radius: 0px;
    font-family: MuseoSans-500 !important;
    background: rgb(248, 248, 248);
    padding: 15px 30px;
}
.collection-accordion-header {
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    align-items: center;
    transition: background 0.3s;
    font-weight: 600;
    font-size: 15px;
    line-height: 22px;
    font-family: MuseoSans-500 !important;
}
.collection-accordion-header:hover {
  color: #166e5f;
}
.collection-accordion-icon {
  font-size: 22px;
  color: #166e5f;
  transition: transform 0.3s ease;
}
.collection-accordion-item.active .collection-accordion-icon {
  transform: rotate(45deg);
}
.collection-accordion-panel {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease, padding 0.3s ease;
  padding: 0;
    font-size: 16px;
    line-height: 24px;
    color: var(--color-text-body);
}
.collection-accordion-item.active .collection-accordion-panel {
  max-height: 1000px;
  padding: 15px 0px 0px;
}
</style>

{% javascript %}
document.addEventListener("DOMContentLoaded", function() {
  const headers = document.querySelectorAll(".collection-faq-accordion .collection-accordion-header");
  headers.forEach(header => {
    header.addEventListener("click", function() {
      const item = this.closest(".collection-accordion-item");
      // Only one open at a time
    //   document.querySelectorAll(".collection-accordion-item").forEach(i => {
    //     if (i !== item) i.classList.remove("active");
    //   });
      item.classList.toggle("active");
    });
  });
});
{% endjavascript %}

{% schema %}
{
  "name": "Collection FAQ Accordion",
  "settings": [
    {
      "type": "text",
      "id": "section_title",
      "label": "Section Title",
      "default": "Frequently Asked Questions"
    }
  ],
  "presets": [
    {
      "name": "Collection FAQ Accordion"
    }
  ]
}
{% endschema %}
